name: falcbot
channels:
  - conda-forge
  - defaults

dependencies:
  # Base depends
  - python
  - pip

  # Testing
  - pytest
  - pytest-cov
  - codecov

  - pydantic<2.0a0

  # slack
  - slack-bolt

    # Pip-only installs
  #- pip:
  #  - codecov

